import "./SearchBar.css";

function SearchBar() {
  return (
    <div>
      <input type="text" placeholder="search books" name="search"></input>
      <button>search</button>
    </div>
  );
}

export default SearchBar;
